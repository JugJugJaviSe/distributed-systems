server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  # Serve files if they exist, otherwise fall back to index.html (SPA routing)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
